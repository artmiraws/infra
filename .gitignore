# OpenTofu / Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Variable values (may contain sensitive data)
*.tfvars
*.tfvars.json
!*.tfvars.example

# Credentials and local environment
.env
.envrc
*.pem
*.key

# Local backend configuration (contains the state bucket name)
backend.hcl

# Editor / OS
.DS_Store
.idea/
.vscode/
